<% if defined?(image) && image %>
    <%= image_tag image.attachment.url(:product), :itemprop => "image" %>
<% elsif @product.videos.any? %>
    <%= embed(@product.videos.first.attachment, "image") %>
<% else %>
    <%= product_image(@product, :itemprop => "image") %>
<% end %>
